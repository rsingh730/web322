<div class="row">
    <div class="col-md-12">
        <h2>Items</h2>
        <hr />

        <!-- Add Item button -->
        <a href="/Items/add" class="btn btn-success pull-right">Add Item</a>

        <!-- Display the table of items -->
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Item ID</th>
                        <th>Title</th>
                        <th>Item Date</th>
                        <th>Price</th>
                        <th>Category</th>
                        <th>Published</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each items}}
                        <tr>
                            <td>{{id}}</td>
                            <td>{{title}}</td>
                            <td>{{formatDate itemDate}}</td> <!-- Formatting date -->
                            <td>{{price}}</td>
                            <td><a href="/items?category={{category}}">{{category}}</a></td>
                            <td>{{#if published}}Yes{{else}}No{{/if}}</td> <!-- Show Yes/No for published -->
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>

    <!-- Display error message if no items are found -->
    <div class="col-md-12 text-center">
        {{#unless items}}
            <strong>{{message}}</strong>
        {{/unless}}
    </div>
</div>
